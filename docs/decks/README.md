# Слайдові колоди уроків (L01.pptx…L12.pptx)

Службовий матеріал, як [PRODUCTION.md](../longreads/PRODUCTION.md): студентам не
видається, продукт курсу лишається текстовим. Колоди потрібні тому, хто **веде
заняття або записує відео**. Комплект уроку N: `../longreads/LNN.html` — джерело
змісту, `LNN.pptx` — колода, `LNN-script.md` — начитка.

Зміст простежується до лонг-ріда; демо-кадри й застереження ведучому — з
PRODUCTION; формат і структура — за корпоративним регламентом навчальних
презентацій Neoversity (v1.0).

## Структура колоди

Титул → learning outcomes (дієсловами дії) → карта уроку → **терміни уроку**
(pre-training) → **розділювач ТЕОРІЯ** → блоки лонг-ріда → **розділювач
ПРАКТИКА** → місток «теорія → демо» → лабораторна → **розділювач РЕФЛЕКСІЯ** →
«що це довело» → «перевір себе» → антипатерни → ДЗ → підсумок і наступний крок.

Маркери активності в шапці кожного слайда: ТЕОРІЯ (absorb), ПРАКТИКА (do),
РЕФЛЕКСІЯ (connect). Опційні блоки позначені бейджем ОПЦІЙНО.

**Три розділювачі на урок** стоять рівно на межах цих трьох фаз — по одному
слайду темного градієнта з одним великим словом. Це найчастіший архетип
шаблону (16 із 34 його прикладів). У шаблоні розділювачі займають 47%
слайдів, у нас — 11% (36 із 324): там це презентація-вступ із розрідженим
змістом, тут — лекція, і розділювач на кожному другому слайді розірвав би
виклад. Межі вибрані не на око: вони збігаються з маркерами absorb / do /
connect, які вже стоять у шапках.

РЕФЛЕКСІЯ стоїть на трьох слайдах кожного уроку — «що це довело», «перевір
себе» і антипатерни: усі три просять учасника співвіднести побачене з власною
системою, а не засвоїти нове.

## Візуальний стандарт

Смислові схеми замість текстових карток: потоки з відгалуженнями, машини станів,
таймлайни, порівняльні смуги, шари стека, зони «видимо / невидимо», великі
цифри-акценти. Один смисловий акцент на слайд; ~70 слів тексту на слайд;
дві гарнітури.

**Структура — за архетипами шаблону.** У шаблоні 42 слайди, з них 8 службові
(інструкція, бібліотека 3D-ассетів, каркаси схем), решта 34 — приклади:
16 темних розділювачів з одним великим заголовком, **11 білих карток 91% площі
з чорним текстом**, 5 чорних полотен із білими плитками, 2 темні з
текстом-визначенням. Тобто зі 18 змістових слайдів 11 — біла картка. Звідси:

- **контентні слайди — біла картка** `x 0.26 y 0.21 12.81×7.07` (91%), чорний
  заголовок, чорний текст. Пряме правило з інструкції шаблону (сл. 1):
  «використовуйте чорний колір для основного шрифту»;
- плашки лежать НА картці: **чорні з білим текстом** (сл. 16), акцентна —
  **один** суцільний `#5A05F4` з білим (сл. 10, 15), виноски — білі рядки
  з тонкою рамкою (сл. 13, 40, 41);
- **обкладинка й підсумок** — темний градієнт із ассетів шаблону, білим по
  темному; на обкладинці скляна «N» — той самий ассет, що в шаблоні на сл. 9 і 42;
- **розділювачі** — темний градієнт з одним словом: Unbounded великими 65 pt
  (рівно кегль «ПРАКТИКИ» на сл. 28 шаблону), короткий підпис 15 pt і за потреби
  фіолетовий чип «→ що далі» (як на сл. 38). Ні номера, ні лого — у шаблонних
  розділювачах їх немає;
- **зелений і червоний — тільки точково**, на «правильно / помилка», і лише
  кольором підпису та тонкою рамкою, а не заливкою. Коричневого й лавандового
  немає: у шаблоні таких кольорів не існує.

Частки барв у рендері зведені з шаблоном: фіолет 54.9% насиченої площі проти
52.3% у шаблоні, бордо 25.9 проти 28.6, синій 14.0 проти 16.3, бірюзи 0.02
проти 0.13. Перевіряється `_build/make_bg.py` (фони) і обходом рендера.

П'ять тонів (`card / acc / good / warn / crit`) відрізняються не п'ятьма
заливками, а кольором ПІДПИСУ на чорній плашці. Ключі `plate` і `card` —
різні сутності: `plate` — чорна плашка-блок, `card` — біла панель із рамкою,
яку генератори малюють самі. Поки ключ був один, темний текст лягав на чорне.
Для тих самих ролей на білому тлі є `TONE_ON_CARD` (темні варіанти), бо світлі
читаються лише на чорній плашці.

**Три майстри** — по одному на тип фону (контент / обкладинка / фінал), фон
лежить у майстрі, тож кожне зображення входить у файл один раз.

Функціональність палітри перевіряється буквально: якщо виділення не можна
пояснити словами, його не має бути. На карті уроку кольором позначено лише
опційний блок; виняток — L01, де фіолетовим відділено «об'єкт керування» від
«каркаса», і це сказано підписом під картою.

Фони — два готові, під різні типи слайдів, як у шаблоні: контентний (чистий
градієнт) і обкладинковий (плюс вордмарк NEOVERSITY і бордовий акцент праворуч).
Обидва складає `_build/make_bg.py` з ассетів самого шаблону. Складання поважає
`<a:srcRect>` і `rot` у тому порядку, що й OOXML (обрізка джерела → вписування
в рамку → поворот): якщо крутити до обрізки, у кадр потрапляє інший фрагмент
`image3` — саме там лежать зелений, бірюзовий і жовтий, яких у шаблоні на екрані
немає. Верхи яскравості підтиснуті так, щоб найтемніший текст, що лежить прямо
на фоні, тримав 4.5:1.

## Начитка

`LNN-script.md` — повний текст виступу поблочно на кожен слайд
(«## Слайд N · назва»). Помітки `[Ведучому: …]` — граблі зі стенда (порядок
кадрів, прогрів, стани, які треба скинути перед дублем); вони для того, хто
записує, і на слайди не виносяться. У самих `.pptx` нотаток немає.

Орієнтир хронометражу: 31–39 хвилин начитки на урок, ~48,5 тисяч слів разом
(324 слайди у 12 колодах). Блоки розділювачів короткі й навмисно: це пауза
на кілька секунд, а не окрема теза.

**Блоки роздаються слайдам позиційно.** Зайвий або пропущений блок зсуває всю
решту начитки і обрізає хвіст, а `save()` перезаписує `.md` заголовками слайдів
— тому після такого зсуву файл виглядає консистентним. Збірка тепер падає, якщо
блоків і слайдів різна кількість, і попереджає, якщо заголовок блоку розійшовся
з назвою слайда. Попередження нормальне рівно тоді, коли ви щойно перейменували
слайд; у решті випадків це зсув — не ігноруйте його.

## Правила підтримки

- Колоди прив'язані до поточного стану лонг-рідів: якщо `LNN.html` редагується,
  відповідну колоду треба перезібрати, інакше слайди розійдуться з уроком.
- **Потрібні шрифти: Unbounded (заголовки), Montserrat (текст), Courier New
  (код).** Без Unbounded PowerPoint підставить іншу гарнітуру — заголовки
  поїдуть, а перевірка переносу стане недійсною: логіка `titleLines()` у
  `_build/deck_lib2.js` рахує саме метрики Unbounded. Обидва вільні: Unbounded
  і Montserrat є на Google Fonts під OFL.
- Верстка перевіряється рендером **PowerPoint** (`_build/pptx_export.ps1`) —
  LibreOffice підміняє гарнітури й показує інший перенос рядків.
- Дві автоматичні перевірки перед комітом:
  `python _build/overlap.py ../L*.pptx` — накладання й виходи за межі (рахує
  справжню висоту таблиць як суму рядків і бачить зображення, бо на цих двох
  дірах повз нього двічі проходили дефекти);
  `python _build/contrast.py ../L*.pptx` — WCAG для кожного рану проти його
  СПРАВЖНЬОЇ поверхні, включно з фоном у тій його ділянці, де лежить текст;
  `python _build/gaps.py` — порожнина внизу білої картки. Зазор понад 1.2"
  читається як незакінчений слайд; норма — близько 0.5". Зараз таких немає,
  максимум 1.17". Перевірка потрібна тому, що сітка кожного патерна задана
  числами в генераторі: щойно блок скорочується, слайд «підіймається» вгору,
  а порожнина внизу нікого не ламає й тихо лишається.
- Правило сторінок уроку «жодних шляхів до файлів матеріалів» діє і тут:
  на слайдах лишаються тільки шляхи проєкту студента і `localhost:*`.
- Схеми, зібрані для слайдів (порівняльні смуги цін і часова смуга латентності —
  обидві в L01), підписані як схематичні: у джерелі точних коефіцієнтів немає,
  тож обидві потребують підтвердження SME перед першим записом.
